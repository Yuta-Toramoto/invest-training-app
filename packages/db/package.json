{
  "name": "@invest-training/db",
  "version": "0.0.1",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "clean": "rm -rf dist .drizzle"
  },
  "dependencies": {
    "@invest-training/types": "workspace:*",
    "drizzle-orm": "^0.43.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@invest-training/config": "workspace:*",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.31.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0"
  }
}
